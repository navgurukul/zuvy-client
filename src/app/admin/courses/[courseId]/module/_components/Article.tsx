import React, { useState } from 'react'

function Article({ content }: { content: Object }) {
    const [articleContent, setArticleContent] = useState([])
    return <div>Article</div>
}

export default Article
