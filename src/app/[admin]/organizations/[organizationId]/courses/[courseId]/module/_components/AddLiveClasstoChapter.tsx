import React from 'react'
import { AddLiveClasstoProps } from '@/app/[admin]/organizations/[organizationId]/courses/[courseId]/module/_components/ModuleComponentType'

const AddLiveClasstoChapter = ({
    logSelectedRows,
    table,
}: AddLiveClasstoProps) => {
    const selectedRows = logSelectedRows()

    return <div>AddLiveClasstoChapter</div>
}

export default AddLiveClasstoChapter
